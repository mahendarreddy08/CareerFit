import { Results } from "@/components/results"

export const metadata = {
  title: "Results — CareerFit",
  description: "Your career fit analysis results.",
}

export default function ResultsPage() {
  return <Results />
}